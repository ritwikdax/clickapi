{
  "name": "clickapi-monorepo",
  "version": "1.0.0",
  "description": "",
  "main": "index.ts",
  "scripts": {
    "cleanup": "rm -rf node_modules example/node_modules lib/node_modules package-lock.json && npm install",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "private": true,
  "workspaces": [
    "lib",
    "example"
  ],
  "devDependencies": {
    "@types/node": "^22.15.30",
    "typescript": "^5.8.3"
  }
}
